import type { ModuleConfig } from '../../lib/module-loader'

export const moduleConfig: ModuleConfig = {
  id: 'itemshop',
  name: 'Item Shop',
  enabled: true,
  version: '1.0.0',
  fallback: 'Item-Shop vorübergehend nicht verfügbar',
}
