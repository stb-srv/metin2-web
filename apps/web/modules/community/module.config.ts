import type { ModuleConfig } from '../../lib/module-loader'

export const moduleConfig: ModuleConfig = {
  id: 'community',
  name: 'Community',
  enabled: true,
  version: '1.0.0',
  fallback: 'Community-Bereich vorübergehend nicht verfügbar',
}
